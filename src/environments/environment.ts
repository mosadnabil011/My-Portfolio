// Default environment (used by `ng serve`). Values are pulled in here instead
// of being hardcoded inside components, so component code never changes
// between environments and keys live in one place.
export const environment = {
  production: false,
  emailjs: {
    serviceId: 'service_njjpl9d',
    templateId: 'template_h8k05f7',
    publicKey: 'VddB5e6CqwRuzpxmx',
  },
};
